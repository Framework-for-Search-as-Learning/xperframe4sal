# Projeto

Repositório principal que utiliza **Git submodules**.

## Clonar o projeto

Clone o repositório já com os submodules:

```bash
git clone --recurse-submodules https://github.com/Framework-for-Search-as-Learning/xperframe4sal.git
```

Se já clonou sem os submodules, execute:

```bash
git submodule update --init --recursive
```

